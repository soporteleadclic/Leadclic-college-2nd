 public static List<Contact> changecontactemail(List<Opportunity> newList) {

        ID[] opportunityId = new list<ID>();
        for (Integer i = 0; i < newlist.size(); i++) {
         opportunityId.add(newList[i].AccountID);
            system.debug(newList[i].AccountID);
        }
        List<Account> accounts =[SELECT Id FROM Account  WHERE Type LIKE 'Other' AND Id IN :opportunityId];
        
     opportunityId.clear();
        for (Integer i = 0; i < accounts.size(); i++) {
         opportunityId.add(accounts[i].Id);
        }
        List<Contact> contacts =[SELECT FirstName,Email
        FROM Contact WHERE AccountID IN :opportunityId ORDER BY CreatedDate DESC LIMIT 50 ];
        
        List<Contact> contactsNewList = new List<Contact>();
        for ( Integer i = 0; i <contacts.size();i++) {
            if(contacts[i].Email== null|| contacts[i].Email==''){
                contacts[i].email='sincontacto@example.org';
                contactsNewList.add(contacts[i]);
                
            }
        } 
        update contactsNewList; 
        return contactsNewList;
    }
